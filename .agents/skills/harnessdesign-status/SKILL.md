---
name: harnessdesign-status
description: "Codex entrypoint for viewing the current HarnessDesign task status."
---

This skill mirrors `/harnessdesign-status` for Codex.

Use the `harnessdesign_runtime` tools to enumerate tasks and load the task summary, and run `python3 scripts/validate_transition.py --summary <task_dir>` when a deterministic summary is required by the SOP.

Arguments: $ARGUMENTS
